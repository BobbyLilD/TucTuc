import { Config } from './src/types';

const config: Config = {
  apiGateway: {
    url: 'https://handwriter.ru/api',
  },
  wsAddress: 'wss://handwriter.ru/ws',
  metricaId: 0,
};

export default config;
